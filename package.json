{
  "name": "hello-kitty-friends-cromos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "cap:sync": "npm run build && npx cap sync android",
    "android": "npx cap open android",
    "android:build": "npm run build && npm run android:prepare && cd android && ./gradlew assembleDebug",
    "android:release": "npm run build && npm run android:prepare && cd android && ./gradlew assembleRelease",
    "android:prepare": "node scripts/prepare-android.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@capacitor/core": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor-community/admob": "^8.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@vercel/node": "^5.0.0",
    "@capacitor/assets": "^3.0.5"
  }
}
